/**
 * 
 */
//所有弹出窗体公用属性
if ($.fn.window){
	//$.fn.window.defaults.iconCls='icon-save';
	$.fn.window.defaults.minimizable=false;
	$.fn.window.defaults.collapsible=false;
	//$.fn.window.defaults.closed=true;
	$.fn.window.defaults.modal=true;
}